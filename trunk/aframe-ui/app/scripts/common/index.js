'use strict';

angular.module(APP_NAME + '.common', [
    APP_NAME + '.common.code',
    APP_NAME + '.common.error',
    APP_NAME + '.common.itrust',
    APP_NAME + '.common.layout',
    APP_NAME + '.common.menu'
]);
